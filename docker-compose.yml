services:
  db:
    image: postgres:15
    container_name: student_db
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=ranking_db
    ports:
      - "5432:5432"